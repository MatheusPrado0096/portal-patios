/**
 * Configurações gerais do portal e do visualizador PDF.
 */
export const PDF_CONFIG = {
  minZoom: 0.15,
  maxZoom: 8,
  zoomFactor: 1.25,
  fitMaxZoom: 4,
  minimumPixelRatio: 2,
};

export const THEME = {
  light: "light",
  dark: "dark",
};
